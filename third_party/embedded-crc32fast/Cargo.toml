[package]
name = "crc32fast"
version = "1.5.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Small no_std CRC-32 implementation for flash-constrained firmware"

[lib]
path = "src/lib.rs"

[features]
default = []
std = []
